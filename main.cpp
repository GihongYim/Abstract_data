#include <iostream>
#include "vector.hpp"


int main(void)
{
    ft::vector<int> v(10);

    // v.push_back(1);
    // v.push_back(2);
    // for (unsigned long i = 0 ; i < v.size(); i++) {
    //     std::cout << v[i] << ' ';        
    // }
    // std::cout << std::endl;
    
    return 0;
}